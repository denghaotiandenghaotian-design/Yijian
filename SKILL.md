# 一建考试全能备考系统 · WorkBuddy 技能包

> 一级建造师（四科）**开箱即用**学习复习系统 · 基于 WorkBuddy 项目框架
> 版本 v1.0 | 2026-08-12 | 覆盖经济 / 法规 / 管理 / 建筑实务
> 形态：**纯前端单页应用（SPA）**，双击 `index.html` 即可运行，无需服务器与依赖安装。

---

## 一、系统定位

本技能包将「一建全能备考 AI」的 12 大模块收敛为**可直接运行**的 Web 应用，
聚焦用户指定的五大核心功能：

| 用户需求 | 对应模块 | 能力 |
|----------|----------|------|
| 题库练习 | 智能刷题 | 随机/薄弱优先组卷、即时判分、解析、薄弱章节诊断 |
| 章节复习 | 考点库 + 思维导图 | 按官方教材章节树浏览考点、速记卡 |
| 错题收藏 | 错题回顾 | 错题本（顽固排序）、收藏（★）、错题重做（连对消灭） |
| 模拟考试 | 全真模考 | 限时/不限时、真实题型·分值·评分规则、成绩报告 |
| 学习进度跟踪 | 打卡学习 | 学习力指数、SVG 趋势图、成就里程碑 |

---

## 二、框架目录结构（WorkBuddy 约定）

```
一建/
├── index.html              # 唯一入口（浏览器打开）
├── README.md               # 快速启动
├── SKILL.md                # 本文件
├── assets/
│   ├── css/styles.css      # 视觉规范（深蓝 #0e1630 + 金 #e2a252）
│   └── js/
│       ├── data_economy.js / data_law.js / data_management.js / data_practice.js
│       │                   # 各科目题库 + 章节考点，挂载于全局 window.YJ_DATA
│       └── app.js          # 应用引擎 + 五大模块（IIFE，零外部依赖）
└── data/                   # 知识点文档（公式库/口诀库/知识库/考点速查）
```

**编码规范**
- 数据层：纯浏览器全局对象（`window.YJ_DATA`），通过 `<script src>` 同步加载，兼容 `file://`。
- 应用层：单文件 IIFE，`"use strict"`，无 ES module / 无 fetch / 无 CDN，确保离线可运行。
- 持久化：浏览器 `localStorage`（key `yj_state_v1`），结构含 `attempts / wrong / stats / mock / achievements / streak`。
- 图表：手写 SVG，无第三方库。
- 启动期对数据做字段归一化（`stem`↔`content`、`answer` 数组↔字符串、`explanation`↔`analysis`），提升题库鲁棒性。

---

## 三、触发与运行

**用户说「打开/启动一建备考系统」「刷题」「来套模考」「看下我的进度」等时：**
1. 定位到工作区 `C:\Users\dht\WorkBuddy\一建\`；
2. 直接用浏览器打开 `index.html`（或 `python -m http.server` 后访问）；
3. 如需扩充题库，按 `README.md` 的题对象格式向 `assets/js/data_*.js` 的 `questions` 追加即可。

**评分规则（官方对齐）**
- 单选：选对 1 分，错选 0 分。
- 多选：全对 2 分；无错选时每正确选项 0.5 分；有错选 0 分。
- 实务案例：按采分点给分（模考中由用户自评满分/半分/0 分）。
- 及格线：经济 60 / 法规 78 / 管理 78 / 实务 96（卷面满分 60%）。

---

## 四、内置数据资产

- 题库：四科各 25 题（经济/法规/管理：单选 15 + 多选 10；实务：单选 15 + 多选 5 + 案例 5），合计 100 题，均含标准答案与解析。
- 章节考点：每章 `knowledge` 数组内置高频考点要点，支撑章节复习与速记卡。
- 知识点文档：`data/` 下四份 Markdown（公式库/口诀库/知识库/建筑考点速查），供深度查阅。

> 内置题为高质量样例，用于打通完整学习闭环；正式备考建议持续扩充至贴近真题规模。
